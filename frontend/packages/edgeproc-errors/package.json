{
	"name": "@edgeproc/errors",
	"private": true,
	"version": "0.1.0-dev",
	"license": "MIT",
	"type": "module",
	"description": "Canonical error glue: register a per-app catalog, classify raw failures into stable codes, describe via your own i18next, serialize to RFC 9457 Problem Details. Zero runtime deps. Vendored from the edgeproc/errors repo (the portfolio canonical-errors standard).",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage"
	},
	"devDependencies": {
		"@types/node": "^26.1.0",
		"@vitest/coverage-v8": "^4.1.10",
		"typescript": "^6.0.3",
		"vitest": "^4.1.10"
	}
}
