{
	"name": "aml-filter-frontend",
	"private": true,
	"version": "2.1.0",
	"description": "AML-Filter v2 frontend — admin dashboard + in-browser OFAC screening tier.",
	"type": "module",
	"packageManager": "pnpm@11.5.0",
	"engines": {
		"node": ">=22.13"
	},
	"scripts": {
		"publish-list": "pnpm --filter @amlfilter/publisher run publish",
		"build-demo-list": "pnpm --filter @amlfilter/publisher run build-demo"
	},
	"devDependencies": {
		"jsdom": "^29.1.1"
	}
}
