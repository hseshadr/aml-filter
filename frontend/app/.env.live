# `vite --mode live` (pnpm dev:live / build:live): screen against the REAL
# OFAC / UN / EU / UK lists mirrored from aml-filter.com into the gitignored
# public/bundle/live by `pnpm bundle:live`, verified against public.key.
VITE_BUNDLE_BASE_URL=/bundle/live
