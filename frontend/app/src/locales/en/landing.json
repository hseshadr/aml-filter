{
	"hero": {
		"eyebrow": "on-device · in your browser · no backend",
		"title": "Sanctions screening that runs <em>entirely in your browser</em>.",
		"lede": "Screening a name usually means shipping your customers’ names to a third-party SaaS. AML-Filter turns that inside out. The sanctions list syncs in as a signed bundle, is ed25519-verified in the tab, and the matcher runs locally — so nothing you type leaves the device. Private, backend-free, and free to operate.",
		"audience": "For small and medium-sized businesses with KYC obligations: onboard customers, screen sanctions locally, and keep customer data on the device.",
		"ctaPrimary": "▶ Try the live demo",
		"ctaSecondary": "Open the workstation",
		"footnote": "First load fetches the matcher model plus the signed bundle; after that both are cached, so later visits start fast. You still need a network connection to open the page — there is no offline mode."
	},
	"metrics": {
		"ariaLabel": "Metrics",
		"title": "What it costs to run",
		"note": "measured / real figures",
		"infra": {
			"label": "backend infra to run the demo",
			"sub": "no API, no vector DB, no servers in the path"
		},
		"matcher": {
			"label": "MiniLM matcher, cached once",
			"sub": "all-MiniLM-L6-v2 quantized ONNX, run in the tab"
		},
		"entities": {
			"label": "sanctions entities on the demo list",
			"sub": "fictional demo bundle · the workstation loads real lists"
		},
		"pii": {
			"label": "of what you type leave the device",
			"sub": "the name is matched entirely in the tab"
		}
	},
	"whys": {
		"ariaLabel": "Why",
		"private": {
			"title": "Private",
			"body": "The name you screen is matched in the tab and never leaves the device. No query, no customer name, is sent to a third-party SaaS."
		},
		"local": {
			"title": "Local",
			"body": "Backend-free and in-tab. The model and the signed bundle download once and are then cached, so screening itself never calls a server. Loading the page does need a network connection — there is no offline mode."
		},
		"verifiable": {
			"title": "Verifiable",
			"body": "The sanctions list arrives as a content-addressed, ed25519-signed bundle, verified fail-closed in-tab against a pinned key. A bad signature aborts the load."
		}
	},
	"howItWorks": {
		"ariaLabel": "How it works",
		"title": "How it works",
		"steps": {
			"sync": "sync the signed bundle",
			"verify": "verify ed25519 + sha-256 in-tab (fail-closed)",
			"load": "load MiniLM once",
			"embed": "normalize + embed the query",
			"score": "transparent weighted scoring",
			"rank": "ranked matches with per-signal reasons"
		}
	},
	"workstation": {
		"ariaLabel": "Compliance workstation",
		"kicker": "the local-first KYC tier",
		"title": "Need the full workflow? The KYC workstation runs in your browser too.",
		"lede": "Onboard customers, auto-screen them against the signed sanctions bundle, and triage tiered matches on a review board — persisted to SQLite in your browser’s private storage (OPFS). No login, no server: your customer data never leaves the device.",
		"flow": {
			"onboard": "Onboard a customer",
			"screen": "Auto-screen in-tab",
			"review": "Tiered review board",
			"resolve": "Resolve with an audit trail"
		},
		"cta": "Start onboarding"
	}
}
