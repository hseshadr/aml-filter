node_modules
dist
coverage

# Self-hosted MiniLM embedder weights (~23 MB), materialized by the `prebuild`
# hook (scripts/download-model.mjs) before every `pnpm build`. Kept out of git so
# the repo stays lean; `pnpm build` repopulates them locally.
public/models/

# Self-hosted onnxruntime-web wasm runtime (~23 MB), staged by the `prebuild`
# hook (scripts/stage-ort-wasm.mjs) from the lockfile-pinned node_modules copy —
# same policy as the model weights above.
public/ort/
playwright-report
test-results
*.log
.DS_Store
