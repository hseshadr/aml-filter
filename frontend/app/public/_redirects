# Each SPA route has a Vite HTML entrypoint, so Pages can serve deep links without
# a catch-all 200 rewrite. The top-level 404.html handles every unknown path.
/screen/ /screen 301
/customers/ /customers 301
/review/ /review 301
/settings/ /settings 301
